# Backend Environment Variables
PORT=5000
NODE_ENV=development

# CORS - Update this in production to your Vercel frontend URL
CORS_ORIGIN=http://localhost:3000

# yt-dlp binary path (Render uses /usr/local/bin/yt-dlp)
YT_DLP_PATH=/usr/local/bin/yt-dlp

# Cookies file path (Render stores secrets in /etc/secrets/)
COOKIES_PATH=/etc/secrets/cookies.txt